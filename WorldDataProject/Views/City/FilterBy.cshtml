@{
  Layout = "_Layout";
}

  <div class='container'>
      <div class='row'>
          @{
               await Html.RenderPartialAsync("_CityForm");
           }
               @foreach (var city in Model)
               {
                  <div class='panel panel-default'>
                      <div class='panel-heading'>@city.GetName()</div>
                      <div class='panel-body'>
                      @city.GetFilterValue()<br/>
                      </div>
                  </div>
               }
       </div>
  </div>
